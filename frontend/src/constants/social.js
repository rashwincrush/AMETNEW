// Feature toggles for social sharing (link-out only; no tokens)
export const SOCIAL_ENABLED = true;
export const ENABLE_PLATFORMS = {
  twitter: true,
  facebook: true,
  linkedin: true,
};

// If needed later, per-route gating can be derived at call sites.
