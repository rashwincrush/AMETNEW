### TODO

##### Jobs portal end-to-end audit (OPIB-∞)
- [ ] Map FE components and flows for jobs (listings, post quick/full, edit, detail, apply, manage)
- [ ] Trace BE/API/RPC used (Supabase RPCs, tables) for create/update/read job listings
- [ ] Review job alerts flow (FE + RPCs + table schema) and notification dispatch
- [ ] Audit permissions/RLS/role handling for admin/student/alumni/employer across flows
- [ ] Identify gaps/security/QA edge cases and produce OPIB-∞ structured report

1. Verify degrees (B.Com, M.Com Finance).
2. Confirm “Match my education” fetch uses updated search_jobs_with_education (degree + department) and returns results; add empty-state hint if needed.
3. Implement unified Job form where CREATE (Quick Link + In-App) and EDIT share same field names and payloads.
